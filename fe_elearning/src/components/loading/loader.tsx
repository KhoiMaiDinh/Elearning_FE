'use client';
import React from 'react';

const Loader = () => {
  return (
    <img
      src="/images/logo.png"
      className="my-auto aspect-square w-[48px] shrink-0 animate-spin cursor-pointer self-stretch object-contain"
      alt="News icon"
    />
  );
};

export default Loader;
